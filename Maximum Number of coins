class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        
        piles.sort(reverse=True)
        ans = 0
        while piles:
            piles.pop(0)
            piles.pop(-1)
            ans += piles[0]
            piles.pop(0)
        return ans
